export type AvailableKeys = "ArrowUp" | "ArrowDown" | "ArrowLeft" | "ArrowRight";

export const availableKeysMap: Record<AvailableKeys, string> = {
  ArrowUp: '↑',
  ArrowDown: '↓',
  ArrowLeft: '←',
  ArrowRight: '→',
};
